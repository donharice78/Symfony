<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}My Product Store{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
</head>
<body>
    <header class="bg-gray-800 text-white">
        <div class="container mx-auto px-4 py-4">
            <div class="flex items-center justify-between">
                <div class="text-xl font-semibold">
                    My Product Store
                </div>
                <nav>
                    <ul class="flex space-x-4">
                        <li><a href="{{ path('app_product_index') }}" class="hover:text-gray-300">Home</a></li>
                        <li><a href="{{ path('app_product_index') }}" class="hover:text-gray-300">Products</a></li>
                        {% if app.user %}
                            <li><a href="{{ path('app_logout') }}" class="hover:text-gray-300">Logout</a></li>
                        {% else %}
                            <li><a href="{{ path('app_login') }}" class="hover:text-gray-300">Login</a></li>
                        {% endif %}
                    </ul>
                </nav>
            </div>
        </div>
    </header>
    <main class="py-6">
        {% block body %}{% endblock %}
    </main>
</body>
</html>
